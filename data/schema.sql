CREATE TABLE `procurement_records`
  (
     `purchasing_org`   TEXT,
     `approved_date`    TEXT,
     `approved_time`    TEXT,
     `supplier_contact` TEXT,
     `number`           TEXT,
     `line`             TEXT,
     `category`         TEXT,
     `description`      TEXT,
     `quantity`         REAL,
     `uom`              TEXT,
     `price`            REAL,
     `amount`           REAL,
     `buyer`            TEXT,
     `cancelled`        TEXT,
     `closure_status`   TEXT,
     `misc_attr`        TEXT,
     `type`             TEXT,
     `supplier`         TEXT,
     `site`             TEXT,
     `currency`         TEXT,
     `item_rev`         TEXT,
     `contract_num`     TEXT,
     `ver`              TEXT,
     `supplier_item`    TEXT,
     `item`             TEXT
  );

CREATE TABLE `pcard_records`
  (
     `account_name`               TEXT,
     `posting_date`               TEXT,
     `trans_date`                 TEXT,
     `mcc`                        TEXT,
     `mcc_description`            TEXT,
     `merchant_name`              TEXT,
     `trans_amount`               REAL,
     `ebo`                        TEXT,
     `reports_to`                 TEXT,
     `dcc`                        TEXT,
     `identifier`                 TEXT,
     `sales_tax_amount`           REAL,
     `state_province`             TEXT,
     `city_name`                  TEXT,
     `ship_from_zip`              TEXT,
     `country_code`               TEXT,
     `customer_code`              TEXT,
     `expense_desc`               TEXT,
     `item_desc`                  TEXT,
     `gp`                         TEXT,
     `campus`                     TEXT,
     `dept_activity`              TEXT,
     `fund`                       TEXT,
     `line_item`                  TEXT,
     `function`                   TEXT,
     `project`                    TEXT,
     `task`                       TEXT,
     `award`                      TEXT,
     `expenditure_type`           TEXT,
     `expenditure_org1`           TEXT,
     `expenditure_org2`           TEXT,
     `air_city_of_dest`           TEXT,
     `air_city_of_origin`         TEXT,
     `air_stop_over_indicator`    TEXT,
     `ticket_number`              TEXT,
     `passenger_name`             TEXT,
     `vr_checkout_date`           TEXT,
     `vr_renters_name`            TEXT,
     `lod_arrival_date`           TEXT,
     `lod_total_room_nights`      TEXT,
     `flt_item_description`       TEXT,
     `flt_motor_fuel_amount`      TEXT,
     `flt_motor_fuel_sale_amount` TEXT,
     `flt_motor_fuel_unit_amount` TEXT,
     `item_quantity`              TEXT,
     `extended_amount`            REAL,
     `unit_price`                 REAL,
     `state_or_rc_card`           TEXT
  );

CREATE TABLE IF NOT EXISTS "expense_types"
  (
     "segment_value"      TEXT,
     "description"        TEXT,
     "intermediate_value" TEXT,
     "oasis_object"       TEXT,
     "sub_object"         TEXT,
     PRIMARY KEY("segment_value")
  ) without rowid; 